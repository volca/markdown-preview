{
	"dependencies": {
		"jquery": "^3.6.4",
		"katex": "^0.16.7",
		"marked": "^5.0.4",
		"marked-highlight": "^2.0.0",
		"mermaid": "^10.2.0"
	},
	"scripts": {
		"build": "mkdir -p build && git archive --format zip --output build/archive.zip master",
		"lib": "gulp lib"
	},
	"devDependencies": {
		"gulp": "^4.0.2"
	}
}
