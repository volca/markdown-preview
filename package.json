{
	"dependencies": {
		"jquery": "^3.6.4",
		"katex": "^0.16.7",
		"marked": "^5.0.2",
		"marked-highlight": "^2.0.0",
		"mermaid": "^10.2.0"
	},
	"scripts": {
		"build": "mkdir -p build && git archive --format zip --output build/archive.zip master",
		"add-lib": "node js/add-lib.js"
	},
	"devDependencies": {
		"copyfiles": "^2.4.1"
	}
}
