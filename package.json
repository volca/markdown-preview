{
	"dependencies": {
		"jquery": "^3.6.4",
		"katex": "^0.16.10",
		"marked": "^15.0.8",
		"marked-gfm-heading-id": "^4.1.3",
		"marked-highlight": "^2.2.1",
		"mermaid": "11.6.0"
	},
	"scripts": {
		"build": "mkdir -p build && git archive --format zip --output build/archive.zip master",
		"lib": "gulp lib"
	},
	"devDependencies": {
		"gulp": "^5.0.1",
		"gulp-rename": "^2.1.0"
	}
}
